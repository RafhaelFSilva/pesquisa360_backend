# Arquivos de setores

Coloque nesta pasta os componentes operacionais do Shapefile (`.shp`, `.shx`,
`.dbf` e `.prj`). Eles sao ignorados pelo Git. Consulte
`doc/11-importacao-setores-shapefile.md` para executar a importacao.
